FROM python:3.11

# 필요한 패키지 설치
RUN RUN apt-get update && apt-get install -y libssl3

# 앱 코드 복사
COPY . /app
WORKDIR /app

# Python 패키지 설치
RUN pip install -r requirements.txt

# 앱 실행 명령어
CMD ["python3", "/app/app/data_upload/crawl_update_daily_data.py"]
